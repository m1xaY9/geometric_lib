def area(a):
    return a * a
# Принимает значение а и возврашает его квадрат, то есть площадь квадрата
def perimeter(a):
    return 4 * a
# Принимает значание а и возвращает 4 * а, то есть периметр квадрата